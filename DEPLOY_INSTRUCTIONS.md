# 🚀 Инструкция по деплою Google OAuth на продакшн

## Шаг 1: Настройка Google Cloud Console

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/apis/credentials)
2. Найдите ваш OAuth 2.0 Client ID
3. Нажмите "Edit" (редактировать)
4. В разделе **Authorized redirect URIs** добавьте следующие URL:

```
https://mercadogame.ru/accounts/google/login/callback/
http://mercadogame.ru/accounts/google/login/callback/
https://www.mercadogame.ru/accounts/google/login/callback/
http://www.mercadogame.ru/accounts/google/login/callback/
```

5. Нажмите "Save" (сохранить)

---

## Шаг 2: Подключение к серверу

Подключитесь к вашему продакшн серверу по SSH:

```bash
ssh ваш_пользователь@193.42.125.186
# или
ssh ваш_пользователь@mercadogame.ru
```

---

## Шаг 3: Обновление кода на сервере

Перейдите в директорию проекта и обновите код:

```bash
cd /путь/к/проекту/Mercadogame
git pull origin main
```

---

## Шаг 4: Настройка переменных окружения

Создайте или отредактируйте файл `.env`:

```bash
nano .env
```

Добавьте следующие строки (замените значения на ваши):

```bash
# Google OAuth Credentials (замените на ваши настоящие значения)
GOOGLE_CLIENT_ID=ваш_client_id_из_google_cloud_console
GOOGLE_CLIENT_SECRET=ваш_client_secret_из_google_cloud_console

# Django Settings
DJANGO_DEBUG=False
SECRET_KEY=ваш_секретный_ключ_для_продакшена

# Database Settings (PostgreSQL)
DB_NAME=mercadogame_db
DB_USER=mercadogame_user
DB_PASSWORD=ваш_пароль_базы_данных
DB_HOST=localhost
DB_PORT=5432
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

---

## Шаг 5: Установка зависимостей

Если есть новые зависимости:

```bash
source venv/bin/activate  # Активируйте виртуальное окружение
pip install -r requirements.txt
```

---

## Шаг 6: Применение миграций

Примените миграции базы данных:

```bash
python manage.py migrate
```

---

## Шаг 7: Настройка Google OAuth

Выполните команды для настройки:

```bash
# Обновите информацию о сайте
python manage.py update_site

# Настройте Google OAuth приложение
python manage.py setup_google_oauth
```

Вы должны увидеть сообщения:
```
[OK] Сайт обновлен: mercadogame.ru
[OK] Google OAuth приложение обновлено

=== Google OAuth успешно настроен! ===
```

---

## Шаг 8: Сбор статических файлов

Соберите статические файлы:

```bash
python manage.py collectstatic --noinput
```

---

## Шаг 9: Перезапуск сервера

Перезапустите веб-сервер (зависит от вашей конфигурации):

### Если используется Passenger:
```bash
mkdir -p tmp
touch tmp/restart.txt
```

### Если используется systemd/gunicorn:
```bash
sudo systemctl restart gunicorn
sudo systemctl restart nginx
```

### Если используется supervisor:
```bash
sudo supervisorctl restart mercadogame
```

---

## Шаг 10: Проверка работы

1. Откройте браузер и перейдите на https://mercadogame.ru
2. Нажмите кнопку "Войти"
3. Нажмите "Войти через Google"
4. Выберите аккаунт Google
5. Разрешите доступ к профилю и email
6. Вы должны быть перенаправлены на главную страницу как авторизованный пользователь

---

## ✅ Чеклист деплоя

- [ ] Redirect URIs добавлены в Google Cloud Console
- [ ] Код обновлен на сервере (`git pull`)
- [ ] Файл `.env` настроен с правильными credentials
- [ ] Зависимости установлены
- [ ] Миграции применены
- [ ] Команда `update_site` выполнена
- [ ] Команда `setup_google_oauth` выполнена
- [ ] Статические файлы собраны
- [ ] Веб-сервер перезапущен
- [ ] Google OAuth протестирован на сайте

---

## 🔧 Возможные проблемы и решения

### Проблема: "redirect_uri_mismatch"
**Решение:** Убедитесь, что redirect URI в Google Cloud Console точно совпадает с используемым (включая http/https и слэш в конце).

### Проблема: "invalid_client"
**Решение:** Проверьте, что GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET в .env совпадают с данными из Google Cloud Console.

### Проблема: Токен не сохраняется
**Решение:** Откройте консоль браузера (F12) и проверьте на наличие ошибок JavaScript.

### Проблема: 500 Internal Server Error
**Решение:**
1. Проверьте логи сервера
2. Убедитесь, что DJANGO_DEBUG=False в .env
3. Проверьте, что база данных доступна

---

## 📝 Логи для отладки

Посмотреть логи ошибок Django:

```bash
# Логи Passenger
tail -f /путь/к/логам/passenger.log

# Логи Gunicorn
sudo journalctl -u gunicorn -f

# Логи Nginx
sudo tail -f /var/log/nginx/error.log
```

---

## 🎉 Готово!

Google OAuth теперь настроен и работает на продакшене!

Пользователи могут регистрироваться и входить через свои Google аккаунты.
