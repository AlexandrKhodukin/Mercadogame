# Настройка Google OAuth для MercadoGame

Это руководство поможет вам настроить аутентификацию через Google аккаунт для вашего Django приложения.

## 📋 Содержание

1. [Создание проекта в Google Cloud Console](#1-создание-проекта-в-google-cloud-console)
2. [Настройка OAuth 2.0 Credentials](#2-настройка-oauth-20-credentials)
3. [Настройка переменных окружения](#3-настройка-переменных-окружения)
4. [Настройка Django Admin](#4-настройка-django-admin)
5. [Тестирование](#5-тестирование)
6. [Решение проблем](#6-решение-проблем)

---

## 1. Создание проекта в Google Cloud Console

### Шаг 1.1: Создайте проект

1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Нажмите на выпадающий список проектов (вверху слева)
3. Нажмите **"Новый проект"** (New Project)
4. Введите название проекта: `MercadoGame`
5. Нажмите **"Создать"** (Create)

### Шаг 1.2: Включите Google+ API

1. В меню слева выберите **"APIs & Services"** → **"Library"**
2. Найдите **"Google+ API"**
3. Нажмите **"Enable"** (Включить)

---

## 2. Настройка OAuth 2.0 Credentials

### Шаг 2.1: Настройте OAuth Consent Screen

1. Перейдите в **"APIs & Services"** → **"OAuth consent screen"**
2. Выберите **"External"** (для публичного доступа) или **"Internal"** (только для организации)
3. Нажмите **"Create"**
4. Заполните обязательные поля:
   - **App name:** `MercadoGame`
   - **User support email:** ваш email
   - **Developer contact information:** ваш email
5. Нажмите **"Save and Continue"**
6. На странице **Scopes** нажмите **"Add or Remove Scopes"**
7. Выберите:
   - `.../auth/userinfo.email`
   - `.../auth/userinfo.profile`
8. Нажмите **"Update"**, затем **"Save and Continue"**
9. На странице **Test users** (опционально) добавьте тестовых пользователей
10. Нажмите **"Save and Continue"**

### Шаг 2.2: Создайте OAuth 2.0 Client ID

1. Перейдите в **"APIs & Services"** → **"Credentials"**
2. Нажмите **"+ Create Credentials"** → **"OAuth client ID"**
3. Выберите **Application type:** `Web application`
4. Введите **Name:** `MercadoGame Web Client`
5. Добавьте **Authorized JavaScript origins:**
   ```
   http://localhost:8000
   http://127.0.0.1:8000
   https://mercadogame.ru
   https://www.mercadogame.ru
   ```

6. Добавьте **Authorized redirect URIs:**
   ```
   http://localhost:8000/accounts/google/login/callback/
   http://127.0.0.1:8000/accounts/google/login/callback/
   https://mercadogame.ru/accounts/google/login/callback/
   https://www.mercadogame.ru/accounts/google/login/callback/
   ```

7. Нажмите **"Create"**
8. **ВАЖНО:** Скопируйте и сохраните:
   - `Client ID` (например: `123456789-abc...xyz.apps.googleusercontent.com`)
   - `Client Secret` (например: `GOCSPX-xyz...abc`)

---

## 3. Настройка переменных окружения

### Шаг 3.1: Создайте файл .env

В корневой директории проекта создайте файл `.env` (если его нет):

```bash
# Секретный ключ Django (не используйте в продакшене!)
SECRET_KEY=django-insecure-_^c%ilgp)68ed7#2v*wl5zs&o6pl2k*rxvqu64annuaxwb04c(

# Режим отладки (False в продакшене)
DJANGO_DEBUG=True

# Google OAuth Credentials
GOOGLE_CLIENT_ID=ВАШ_CLIENT_ID_ЗДЕСЬ
GOOGLE_CLIENT_SECRET=ВАШ_CLIENT_SECRET_ЗДЕСЬ

# База данных (необязательно для локальной разработки)
DB_NAME=mercadogame_db
DB_USER=mercadogame_user
DB_PASSWORD=ваш_пароль
DB_HOST=localhost
DB_PORT=5432
```

### Шаг 3.2: Замените значения

Замените `ВАШ_CLIENT_ID_ЗДЕСЬ` и `ВАШ_CLIENT_SECRET_ЗДЕСЬ` на реальные значения из Google Cloud Console.

**Пример:**
```env
GOOGLE_CLIENT_ID=123456789-abcdefghijklmnop.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-1234567890abcdefghijk
```

---

## 4. Настройка Django Admin

### Шаг 4.1: Создайте суперпользователя

Если у вас еще нет суперпользователя:

```bash
cd Mercadogame
venv/Scripts/python.exe manage.py createsuperuser
```

Введите:
- Username: `admin`
- Email: ваш email
- Password: ваш пароль

### Шаг 4.2: Настройте Social Application

1. Запустите сервер разработки:
   ```bash
   venv/Scripts/python.exe manage.py runserver
   ```

2. Откройте браузер и перейдите на: http://127.0.0.1:8000/admin/

3. Войдите с созданными credentials

4. В разделе **"Sites"**:
   - Нажмите на **"example.com"**
   - Измените:
     - **Domain name:** `localhost:8000` (для разработки) или `mercadogame.ru` (для продакшена)
     - **Display name:** `MercadoGame`
   - Нажмите **"Save"**

5. В разделе **"Social applications"** нажмите **"Add social application"**

6. Заполните форму:
   - **Provider:** `Google`
   - **Name:** `Google OAuth`
   - **Client id:** ваш Client ID из Google Console
   - **Secret key:** ваш Client Secret из Google Console
   - **Sites:** Переместите `localhost:8000` (или `mercadogame.ru`) из **"Available sites"** в **"Chosen sites"**

7. Нажмите **"Save"**

---

## 5. Тестирование

### Шаг 5.1: Запустите сервер

```bash
cd Mercadogame
venv/Scripts/python.exe manage.py runserver
```

### Шаг 5.2: Протестируйте вход

1. Откройте браузер и перейдите на: http://127.0.0.1:8000/login.html
2. Нажмите кнопку **"Войти через Google"**
3. Выберите Google аккаунт
4. Разрешите доступ к email и профилю
5. Вы должны быть перенаправлены на главную страницу с авторизацией

### Шаг 5.3: Проверьте данные пользователя

1. Перейдите в Django Admin: http://127.0.0.1:8000/admin/
2. В разделе **"Users"** найдите нового пользователя
3. Проверьте, что email заполнен из Google аккаунта

---

## 6. Решение проблем

### Проблема: "Redirect URI mismatch"

**Решение:**
- Убедитесь, что в Google Cloud Console указаны все правильные redirect URIs
- Проверьте, что в настройках **Sites** в Django Admin указан правильный домен
- Перезапустите сервер после изменений

### Проблема: "Social application not found"

**Решение:**
- Убедитесь, что вы создали Social Application в Django Admin
- Проверьте, что сайт добавлен в "Chosen sites"
- Проверьте SITE_ID в settings.py (должен быть 1)

### Проблема: "Client ID not found in environment"

**Решение:**
- Убедитесь, что файл .env существует в корне проекта
- Проверьте правильность переменных GOOGLE_CLIENT_ID и GOOGLE_CLIENT_SECRET
- Перезапустите сервер

### Проблема: Пользователь создается, но не авторизуется

**Решение:**
- Проверьте настройки AUTHENTICATION_BACKENDS в settings.py
- Убедитесь, что django-allauth правильно настроен
- Проверьте логи сервера на наличие ошибок

---

## 📚 Дополнительная информация

### Полезные ссылки:
- [Google Cloud Console](https://console.cloud.google.com/)
- [django-allauth документация](https://django-allauth.readthedocs.io/)
- [Google OAuth 2.0 документация](https://developers.google.com/identity/protocols/oauth2)

### Структура файлов проекта:

```
Mercadogame/
├── .env                          # Переменные окружения (НЕ КОММИТИТЬ!)
├── mysite/
│   └── settings.py              # Настройки Django (уже настроено)
│   └── urls.py                  # URL маршруты (уже настроено)
├── frontend/
│   ├── login.html               # Страница входа с кнопкой Google
│   ├── register.html            # Страница регистрации с кнопкой Google
│   └── css/
│       └── auth.css             # Стили для кнопки Google
└── manage.py
```

### Что уже настроено в проекте:

✅ django-allauth установлен
✅ Google provider добавлен в INSTALLED_APPS
✅ AUTHENTICATION_BACKENDS настроен
✅ URL маршруты добавлены
✅ Frontend кнопки добавлены
✅ Миграции базы данных выполнены

### Что нужно сделать вручную:

⬜ Создать проект в Google Cloud Console
⬜ Получить Client ID и Client Secret
⬜ Добавить их в файл .env
⬜ Настроить Social Application в Django Admin
⬜ Протестировать вход через Google

---

## 🔒 Безопасность

**ВАЖНО:**
- **Никогда** не коммитьте файл `.env` в Git
- **Никогда** не публикуйте Client ID и Client Secret
- В продакшене используйте HTTPS
- В продакшене установите `DJANGO_DEBUG=False`
- Регулярно обновляйте зависимости

---

## 🚀 Deployment (Продакшен)

Для продакшена на mercadogame.ru:

1. Обновите `.env`:
   ```env
   DJANGO_DEBUG=False
   ```

2. Обновите ALLOWED_HOSTS в settings.py (уже настроено)

3. В Google Cloud Console добавьте production URLs:
   ```
   https://mercadogame.ru
   https://mercadogame.ru/accounts/google/login/callback/
   ```

4. В Django Admin обновите Site:
   - Domain: `mercadogame.ru`

5. Настройте SSL сертификат (Let's Encrypt)

---

**Готово!** Теперь пользователи могут входить через свои Google аккаунты.
